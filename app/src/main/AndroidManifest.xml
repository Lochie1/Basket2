<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.basket.tmail.basket" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.basket.tmail.basket2.mainscreen"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.basket.tmail.basket2.MainMenu"
            android:label="@string/title_activity_menu"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name="com.basket.tmail.basket2.messages"
            android:label="@string/title_activity_messages"
            android:theme="@android:style/Theme.NoTitleBar">
        </activity>
        <activity
            android:name="com.basket.tmail.basket2.email"
            android:label="@string/title_activity_email"
            android:theme="@android:style/Theme.NoTitleBar">
        </activity>
        <activity
            android:name="com.basket.tmail.basket2.Settings"
            android:label="@string/title_activity_settings"
            android:theme="@android:style/Theme.NoTitleBar">
        </activity>
    </application>

</manifest>
